11.20.23 Notes


- When in Print Details, if there are sizes we're not using, please leave boxes blank with zero values so it looks cleaner. Example below (we don't have XXL, 3XL, 4XL, etc. make boxes empty and only show values of shirts we're using)


CREATE TABLE `tasks` (
  `id` int NOT NULL AUTO_INCREMENT,
  `user_id` int DEFAULT NULL,
  `task_detail` varchar(255) DEFAULT NULL,
  `is_checked` int DEFAULT '0',
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`)
);