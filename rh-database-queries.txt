ALTER TABLE `tyler_firm`.`order_transfer` 
ADD COLUMN `shipping_pieces` int NULL AFTER `ink_color_change_prices`;
ALTER TABLE `tyler_firm`.`order_other_charges` 
ADD COLUMN `label_pieces` int NULL AFTER `hang_tag_prices`,
ADD COLUMN `label_prices` float NULL AFTER `label_pieces`,
ADD COLUMN `fold_pieces` int NULL AFTER `label_prices`,
ADD COLUMN `fold_prices` float NULL AFTER `fold_pieces`,
ADD COLUMN `fold_bag_pieces` int NULL AFTER `fold_prices`,
ADD COLUMN `fold_bag_prices` float NULL AFTER `fold_bag_pieces`,
ADD COLUMN `foil_pieces` int NULL AFTER `fold_bag_prices`,
ADD COLUMN `foil_prices` float NULL AFTER `foil_pieces`,
ADD COLUMN `palletizing_pieces` int NULL AFTER `foil_prices`,
ADD COLUMN `palletizing_prices` float NULL AFTER `palletizing_pieces`,
ADD COLUMN `remove_packaging_pieces` int NULL AFTER `palletizing_prices`,
ADD COLUMN `remove_packaging_prices` float NULL AFTER `remove_packaging_pieces`;

CREATE TABLE `quote_approval` (
  `id` int NOT NULL AUTO_INCREMENT,
  `name` varchar(255) CHARACTER SET utf8mb4 DEFAULT NULL,
  `is_active` enum('Y','N') CHARACTER SET utf8mb4 DEFAULT 'Y',
  PRIMARY KEY (`id`) USING BTREE
);

CREATE TABLE `blanks` (
  `id` int NOT NULL AUTO_INCREMENT,
  `name` varchar(255) CHARACTER SET utf8mb4 DEFAULT NULL,
  `is_active` enum('Y','N') CHARACTER SET utf8mb4 DEFAULT 'Y',
  PRIMARY KEY (`id`) USING BTREE
);

ALTER TABLE `tyler_live`.`orders` 
ADD COLUMN `quote_approval` int NULL AFTER `notes`,
ADD COLUMN `blank` int NULL AFTER `quote_approval`;

CREATE TABLE `email_logs` (
  `id` int NOT NULL AUTO_INCREMENT,
  `time_id` int DEFAULT NULL,
  `client_id` int DEFAULT NULL,
  `order_id` int DEFAULT NULL,
  `send_to` varchar(50) DEFAULT NULL,
  `subject` varchar(255) DEFAULT NULL,
  `description` varchar(255) DEFAULT NULL,
  `attachment` varchar(255) DEFAULT NULL,
  `sent_at` int DEFAULT NULL,
  `is_sent` enum('Y','N') DEFAULT 'N',
  `created_by_id` int DEFAULT NULL,
  `created_at` timestamp NULL DEFAULT NULL,
  `updated_at` timestamp NULL DEFAULT NULL,
  PRIMARY KEY (`id`)
);

ALTER TABLE `tyler_live`.`orders` 
ADD COLUMN `internal_notes` text NULL AFTER `notes`;



location dropdown value replaced with Central----->Center



navbar color background : #6aa4e6
Active siderbar li : #041e42
li>a color: #fff

email: white

.sidebar-nav ul li .nav-second-level li a {
    color:white
}

.sidebar-nav ul li.mm-active a {
    color: #fff;
    background-color: #041e42;
    box-shadow: 0 0 10px 1px rgb(255 255 255 / 70%);
}

.btn-success {
    color: #fff;
    background-color: #041e42;
    border-color: #041e42;
}

.sidebar-toggle-icon span {
    display: block;
    position: absolute;
    top: 13px;
    height: 2px;
    min-height: 1px;
    width: 100%;
    border-radius: 0;
    background: #041e42;
}

.btn-success {
    box-shadow: 0 2px 6px 0 rgb(255 255 255 / 50%);
}

a {
    color: #041e42;
    text-decoration: none;
}

.breadcrumb-item.active {
    color: #041e42;
}